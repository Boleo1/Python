# Compose a message using variables and string operators to compose the message correctly.

name = "mikey j"
message = f"Hello, {name.title()} this is Walgreens, your percocet are ready for pick-up."

print(message)